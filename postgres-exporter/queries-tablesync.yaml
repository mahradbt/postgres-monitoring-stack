pg_subscription_table_sync:
  # This runs once per auto-discovered database (see the postgres-exporter-tablesync
  # service in docker-compose.yml) because pg_subscription_rel is NOT a shared
  # catalog - it only shows tables that live in the database you're connected to.
  query: |
    SELECT
      current_database() AS datname,
      sub.subname AS subscription_name,
      n.nspname AS schemaname,
      c.relname AS table_name,
      sr.srsubstate AS state_code,
      CASE sr.srsubstate
        WHEN 'i' THEN 'initializing'
        WHEN 'd' THEN 'copying_data'
        WHEN 'f' THEN 'copy_finished'
        WHEN 's' THEN 'synchronized'
        WHEN 'r' THEN 'ready'
        ELSE 'unknown'
      END AS sync_status,
      CASE sr.srsubstate
        WHEN 'i' THEN 0
        WHEN 'd' THEN 1
        WHEN 'f' THEN 2
        WHEN 's' THEN 3
        WHEN 'r' THEN 4
        ELSE -1
      END AS state_numeric,
      COALESCE(sr.srsublsn::text, '') AS sync_lsn
    FROM pg_subscription_rel sr
    JOIN pg_subscription sub ON sub.oid = sr.srsubid
    JOIN pg_class c ON c.oid = sr.srrelid
    JOIN pg_namespace n ON n.oid = c.relnamespace
  metrics:
    - datname:
        usage: "LABEL"
        description: "Database this subscription belongs to"
    - subscription_name:
        usage: "LABEL"
        description: "Subscription name"
    - schemaname:
        usage: "LABEL"
        description: "Schema of the subscribed table"
    - table_name:
        usage: "LABEL"
        description: "Table name"
    - state_code:
        usage: "LABEL"
        description: "Raw pg_subscription_rel state char (i/d/f/s/r)"
    - sync_status:
        usage: "LABEL"
        description: "Human-readable sync state"
    - state_numeric:
        usage: "GAUGE"
        description: "Numeric encoding of sync state: 0=initializing,1=copying_data,2=copy_finished,3=synchronized,4=ready"
    - sync_lsn:
        usage: "LABEL"
        description: "LSN recorded at last sync state change for this table"

pg_dead_tuples_detail:
  # Also runs once per auto-discovered database, for the same reason as above:
  # pg_stat_user_tables only shows tables in whichever database the connection
  # is in, and the main exporter deliberately never connects to the tenant DBs.
  query: |
    SELECT
      current_database() AS datname,
      schemaname,
      relname AS table_name,
      n_live_tup,
      n_dead_tup
    FROM pg_stat_user_tables
    WHERE n_dead_tup > 0
  metrics:
    - datname:
        usage: "LABEL"
        description: "Database this table belongs to"
    - schemaname:
        usage: "LABEL"
        description: "Schema name"
    - table_name:
        usage: "LABEL"
        description: "Table name"
    - n_live_tup:
        usage: "GAUGE"
        description: "Estimated live row count"
    - n_dead_tup:
        usage: "GAUGE"
        description: "Estimated dead row count (bloat indicator)"
